


# create vm from image
az vm create \
    -g web \
    -n ubuntu-instance-2 \
    --image ${IMAGE_ID} \
    --availability-set web \
    --admin-username ubuntu \
    --nsg xyz-nsg \
    --ssh-key-value azure.pem.pub
    --generate-ssh-keys

